package components

import "github.com/theloosygoose/goserver/internal/view/layout"

templ Card() {
<div class="max-w-56">
    { children... }
</div>
}

templ PhotoContainer() {
    @layout.Base() {
        <div id="main-photo" class="mx-[10%] flex justify-center align-middle mb-[50%] h-dvh md:h-[50%]">
            <p>
               This is a test photo 
            </p>
        </div>
        <div id="photo-container" class="grid grid-flow-col auto-cols-max gap-2 justify-center align-middle">
            { children... }
        </div>
    }
}
