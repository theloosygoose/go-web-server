package components

templ PhotoContainer() {
    <div class="flex-col">
        <div id="main-photo" class="md:mx-[10%] min-h-52 mt-24 mb-16 h-fit">
        </div>
        @Gallery(){
            { children... }
        }
    </div>
}

templ Gallery(){
    <div id="photo-container" class="flex flex-wrap h-fit gap-2 md:gap-3 justify-center">
        { children... }
    </div>
}
